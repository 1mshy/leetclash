{
  "slug": "quiet-stretch",
  "title": "Quiet Stretch",
  "difficulty": "easy",
  "tags": ["two-pointers", "sliding-window", "arrays"],
  "limits": {
    "baseline": { "timeLimitMs": 1000, "memoryLimitKb": 262144 },
    "overrides": {
      "python": { "timeLimitMs": 5000 }
    }
  },
  "starterCode": {
    "python": "import sys\n\ndef main() -> None:\n    data = sys.stdin.read().split()\n    n, s = int(data[0]), int(data[1])\n    a = [int(x) for x in data[2:2 + n]]\n    # TODO: print the length of the longest contiguous stretch with sum <= s\n\nif __name__ == \"__main__\":\n    main()\n",
    "cpp": "#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    long long n, s;\n    cin >> n >> s;\n    vector<long long> a(n);\n    for (auto &x : a) cin >> x;\n    // TODO: print the length of the longest contiguous stretch with sum <= s\n    return 0;\n}\n"
  },
  "publicTests": [0, 1]
}
